[defaults]
# Suppress warnings about PodSecurity violations
# These warnings are expected when using SYS_ADMIN capability with SCC
command_warnings = False
deprecation_warnings = False
system_warnings = False

# Keep other useful output
display_skipped_hosts = False
display_ok_hosts = True

# Made with Bob
